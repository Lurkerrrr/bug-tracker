# Shell scripts must always have LF endings
*.sh text eol=lf

# Dockerfiles too 
Dockerfile text eol=lf
*.dockerfile text eol=lf

# Entrypoint scripts without .sh extension
docker-entrypoint text eol=lf